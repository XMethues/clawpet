[project]
name = "clawchat-pet"
version = "0.5.3"
description = "Hermes activity-driven Petdex growth plugin with selectable gameplay scenes."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pillow>=11.3.0",
]

[tool.uv]
package = false
