# 一个运行时拥有一个产品存档

clawchat-pet 由一个进程内运行时拥有 `save.json`、唯一锁和原子提交。共享成长、当前宠物、宠物人格以及场景/皮肤选择都通过这个事务边界修改；Petdex 文件只作为可重建缓存。Hermes Hook 直接调用该运行时，HTTP 仅适配 `/presentation`、`/catalog`、`/command`、宠物图片和健康检查。短时活动只在内存中存在，重启后回到 idle；时间衰减在活动进入或读取时惰性结算。旧存档和旧 HTTP 接口不迁移、不兼容。
