# Domain Docs

This is a single-context repository.

Before exploring or changing the codebase:

- Read root `CONTEXT.md`.
- Read relevant ADRs under `docs/adr/`.
- Use the glossary’s canonical vocabulary.
- Do not recreate missing domain documents pre-emptively.
- Surface conflicts with an existing ADR explicitly rather than silently overriding it.
